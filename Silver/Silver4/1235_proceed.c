//1235
#include <stdio.h>
#include <string.h>

char str[1003][105] = {};

int main()
{
    int n; scanf("%d", &n);
    
    for(int i = 0; i < n; i++)
    {
        scanf("%s", str[i]);
    }
    
    int length = strlen(str[0]);
    
    for(int i = 1; i <= length; i++)
    {
        int arr[1000005] = {};
    }
}